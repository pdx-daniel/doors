/** Fixed UUID for the seeded org workspace used in local development. */
export const DEV_WORKSPACE_ID = '01900000-0000-7000-8000-000000000001'

/** HTTP header used to scope API requests to a workspace (dev tenancy). */
export const WORKSPACE_ID_HEADER = 'x-workspace-id'
